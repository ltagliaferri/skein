# SKEIN Server Dependencies
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.0.0,<2.12.0; python_version < "3.9"
pydantic>=2.12.0; python_version >= "3.9"
click>=8.1.0
requests>=2.32.0
GitPython>=3.1.0
markdown-it-py>=3.0.0

# Signing (skein/signing.py is shipped library code that imports sigstore;
# sigstore brings cryptography transitively). Runtime dep, not test-only.
sigstore>=4.2,<4.4

# Testing
pytest>=7.0.0
pytest-asyncio>=0.23.0
hypothesis>=6.0.0
